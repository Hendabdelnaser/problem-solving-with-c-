

#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int  x, y, z, one, two;
    cin >> x >> y >> z;
    one = min(x, y);
    two = max(x, y);
    cout << min(one, z) << " " << max(two, z) << endl;







}




